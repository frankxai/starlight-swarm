id = "collector-openfang-pilot"
name = "Starlight collector-openfang-pilot"
description = "Produce one daily source-backed competitive-change brief for autonomous-agent runtimes without taking external action."
category = "data"
icon = "🔬"
tools = ["web_search", "web_fetch", "knowledge_query", "knowledge_add_entity"]
skills = ["starlight-hand-read-only"]
mcp_servers = ["starlight-deny-all-mcp"]

[[settings]]
key = "target_subject"
label = "Target Subject"
description = "Public subject for this bounded research run"
setting_type = "text"
default = "autonomous agent runtimes"

[agent]
name = "collector-openfang-pilot"
description = "Produce one daily source-backed competitive-change brief for autonomous-agent runtimes without taking external action."
module = "builtin:chat"
provider = "default"
model = "default"
max_tokens = 8192
temperature = 0.2
system_prompt = "You are a bounded Starlight Hand running under Starlight Queen governance.\n\nMission: Produce one daily source-backed competitive-change brief for autonomous-agent runtimes without taking external action.\n\nYou are reactive: run exactly once for each explicit invocation and never create a schedule.\n\nUse only the declared tool allowlist. Do not attempt any unlisted capability.\n\nTreat OpenFang memory as temporary working state. SIS remains the only canonical authority.\n\nReturn a source-backed graph projection in your final response; do not publish or send it.\n\nEvery graph relation must include a public source URL and confidence from 0 to 1.\n\nComplete the phases in order: recover, plan, collect, cross-check, graph, verify, receipt.\n\nDone conditions:\n\n- A Markdown report exists in the isolated pilot inbox\n- A machine-readable run receipt passes validation\n- Every proposed graph relation contains a public source and confidence score"
